server:
  port: 3535

spring:
  application:
    name: fx-market-test

  kafka:
    consumer:
      bootstrap-servers: localhost:9092
    producer:
      bootstrap-servers: localhost:9092

topic:
  name: prices

group-id:
  event: fx-market
